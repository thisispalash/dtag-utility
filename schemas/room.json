{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the room"
    },
    "description": {
      "type": "string",
      "description": "The description of the room"
    },
    "hidden_description": {
      "type": "string",
      "description": "The description of the room that is only visible on using 'goggles'"
    },
    "exits": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "direction": {
            "type": "string",
            "description": "The direction of the exit"
          },
          "room": {
            "type": "string",
            "description": "The name of the room that the exit leads to"
          },
          "is_egg": {
            "type": "boolean",
            "description": "If true, the exit direction is redacted"
          }
        }
      }
    },
    "entity": {
      "type": "string",
      "description": "The name of the entity that is in the room"
    },
    "items": {
      "type": "array",
      "description": "The names of the items that are in the room. Should be empty if game has random_spawn set to true",
      "items": {
        "type": "string",
        "description": "The names of the items that are in the room"
      }
    },
    "events": {
      "type": "array",
      "description": "The events that can be triggered in the room based on some conditions",
      "items": {
        "type": "object",
        "properties": {
          "condition": {
            "type": "string",
            "description": "The condition under which the event is triggered",
            "enum": [""]
          },
          "effect": {
            "type": "string",
            "description": "The effect of the event",
            "enum": [""]
          }
        }
      }
    }
  } 
}
